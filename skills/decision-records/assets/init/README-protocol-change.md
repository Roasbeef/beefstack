# Protocol changes

The only way an interface this repo has declared frozen may move. Each
proposal is `NNN-<the-change-as-a-slug>.md`, written before the work that
needs it, and carries Problem, Proposal, Impact and Decision. A proposal
is never edited after acceptance; a further change to the same interface
is a new number.
