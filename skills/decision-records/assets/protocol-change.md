# protocol-change/NNN — <the change as an imperative>

**Status**: PROPOSED YYYY-MM-DD · **Affects**: <spec part / interface> ·
**Raised by**: issue #NN (<why>) · **Implemented**: <packages, once landed>

## Problem

<The state the current interface cannot represent or distinguish, with
measurement. Then, per existing field, why it cannot carry this.>

## Proposal

```
<the interface as it will read afterwards: the field, variant or frame>
```

<Compatibility story if any: optional field, version bump, both ends
ship from one tree.>

## Impact

<Blast radius by language and file. Every generated artifact to
regenerate. Every gate that fails until it is. Operational fallout.>

## Decision

**Accepted.** <The rejected alternative, and why it was dismissed.>
